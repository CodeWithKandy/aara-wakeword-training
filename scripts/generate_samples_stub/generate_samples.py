def generate_samples(*args, **kwargs):
    raise NotImplementedError(
        "Not used — this project generates samples manually via "
        "`python -m piper_sample_generator`. train.py is only ever "
        "run without --generate_clips, so this should never execute."
    )